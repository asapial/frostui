import React from 'react';
import CreateComponentClient from './CreateComponentClient';

const page = () => {
    return (
        <div>
            <CreateComponentClient></CreateComponentClient>
        </div>
    );
};

export default page;